# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.82"
name = "prost"
version = "0.14.3"
authors = [
    "Dan Burkert <dan@danburkert.com>",
    "Lucio Franco <luciofranco14@gmail.com>",
    "Casper Meijn <casper@meijn.net>",
    "Tokio Contributors <team@tokio.rs>",
]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A Protocol Buffers implementation for the Rust Language."
readme = "README.md"
keywords = [
    "protobuf",
    "serialization",
]
categories = ["encoding"]
license = "Apache-2.0"
repository = "https://github.com/tokio-rs/prost"

[lib]
name = "prost"
path = "src/lib.rs"
# https://bheisler.github.io/criterion.rs/book/faq.html#cargo-bench-gives-unrecognized-option-errors-for-valid-command-line-options
bench = false

[features]
default = ["std"]
derive = ["dep:prost-derive"]
no-recursion-limit = []
std = []
indexmap = ["dep:indexmap"]

[dependencies]
byte_str = { version = "0.1.0", path = "../../crates/byte_str" }
bytes = { version = "1", default-features = false }
indexmap = { version = "2.12.1", default-features = false, optional = true }
prost-derive = { version = "0.14.3", path = "../prost-derive", optional = true }
proto-value = { version = "0.1.0", path = "../../crates/proto-value", default-features = false }

[dev-dependencies]
criterion = { version = "0.8", default-features = false }
proptest = "1"
rand = "0.10"
